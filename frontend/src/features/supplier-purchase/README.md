# Supplier & Purchase Order Management (Frontend)

Scope:
- Supplier records (add / edit / view suppliers)
- Purchase orders to suppliers
- Tracking incoming stock from suppliers

All components, pages, and API calls for this module live in this folder.
Shared pieces belong in `src/components/`; shared HTTP setup lives in
`src/services/api.js`.
