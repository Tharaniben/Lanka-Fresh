meta {
  name: Create Category
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/inventory/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Fruits",
    "description": "Fresh fruits and seasonal produce"
  }
}
